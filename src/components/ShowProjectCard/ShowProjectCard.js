import React, { Fragment } from 'react';
import { useStyles } from './ShowProjectCardStyles';

const ShowProjectCard = ({ projectObject }) => {
  const classes = useStyles();

  return (
    <Fragment>
      <h1>HERE</h1>
    </Fragment>
  );
};

export default ShowProjectCard;
